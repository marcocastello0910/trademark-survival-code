# Data are not redistributed here: see the data-availability table in README.md.
# The public sources are downloadable; the Compustat extract is licensed.
data_external/
output/
*.dta
*.csv
*.xlsx

# ...except the two NSF BERD tables, which are a US federal government work
# and small enough to pin here, so the exact release used stays reproducible.
!data/
!data/nsf25354-tab057.xlsx
!data/nsf25354-tab058.xlsx
*.parquet

# Stata and R working files
*.log
*.smcl
.Rhistory
.Rapp.history
.RData
.Rproj.user/

# macOS
.DS_Store

# GitHub account recovery codes: credentials, never to be committed.
github-recovery-codes.txt
*recovery-codes*
